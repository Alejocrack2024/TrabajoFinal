# Sistema de Inventario - Django

# Inicio Rápido

# Prerrequisitos
- Docker
- Docker Compose

# Decargar repo:
git clone https://github.com/tu-usuario/inventario-django.git docker compose up --build
```
levantar solo comando :
cd inventario
crear imagen contenedor:
docker compose build
correr contenedor:
docker compose up -d
desde django:
python manage.py runserver
```

# Acceso:
Aplicación: http://localhost:8000
Panel Admin: http://localhost:8000/admin

El sistema estará ejecutándose en http://localhost:8000
